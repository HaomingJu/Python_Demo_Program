
```
usage: python ./mail.py  --sender_password ***** --receivers demotest@serverurl

optional arguments:
  -h, --help            show this help message and exit
  --stmp_address STMP_ADDRESS
                        邮件服务器地址 smtp.hostname.com
  --stmp_port STMP_PORT
                        邮件服务器端口 465
  --sender_username SENDER_USERNAME
                        发送者邮箱账号 jenkins@trunk.tech
  --sender_password SENDER_PASSWORD
                        发送者邮箱密码
  --mail_context MAIL_CONTEXT
                        邮箱正文
  --receivers RECEIVERS [RECEIVERS ...]
                        收件人邮箱列表
  --attachment ATTACHMENT [ATTACHMENT ...]
                        附件簇列表

```
